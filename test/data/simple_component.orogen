# The name of the component is not the same than the name of the .orogen file,
# THIS IS INTENTIONAL
name "simple"

task_context "SimpleTask" do
end

static_deployment do
    simple_task = task('SimpleTask').
        periodic(0.1).
        start
end

